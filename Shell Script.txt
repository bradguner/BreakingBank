Shell Script

for i in inputs/*.txt
do
	echo "running test $i"
	prog.exe validaccts.txt outputs/$i.sum < $i \ > outputs/$i.log
	python CISC327Asgn2.py //temporary
done

get file
while info left from text
	echo each line
done | python file	< outputs	

for i in inputs/*.txt
do 
	echo "checking outputs for test $i"
	diff outputs/$i.sum expected/$i.sum
	diff outputs/$i.log expected/$i.log
	//pipe into a raw file for outputs
done	